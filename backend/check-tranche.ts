// Scratch diagnostic. Safe to delete. Verified buildPpnDepositTx produces a
// valid VersionedTransaction against the live devnet adapter + LK-90-0430
// vault (notePda derived correctly, 4 ixs, PPN program discriminator
// matches). The sandbox mount blocked my own `rm`.
export {};
