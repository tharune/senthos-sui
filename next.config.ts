import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev indicator (the N badge bottom-right in dev).
  devIndicators: false,
};

export default nextConfig;
